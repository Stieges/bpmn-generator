{
  "profile": "dmn-default",
  "version": "1.0",
  "description": "Default DMN rule profile — the semantic set. Soundness (does the model hold together) plus the semantic warnings that point at something actually wrong. The best_practice layer is off; enable it with mode 'best-practice' or rules/dmn-best-practice-profile.json. Profile format is shared with the BPMN engine (scripts/rule-profile.js).",
  "layers": {
    "soundness": { "enabled": true },
    "semantics": { "enabled": true },
    "best_practice": { "enabled": false }
  },
  "overrides": {}
}
